---
conditions:
  ?
    - @turns == 0
  :
    - begin
  ?
    - @player.hp < 1
  :
    - death
  ?
    - @props['book'].location == 'yard'
  :
    - win
  ?
    - @props['book'].location == 'hallway'
  :
    - loss
outcomes:
  begin: |
    # beginning logic can be put here
  death: |
    @over = true
    output = "You have died.\n"
    output << "Would you like to restart? (y/n)"
  win: |
    # winning logic can be put here
  loss: |
    # losing logic can be put here
