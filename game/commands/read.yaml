syntax:
- "read <prop>"

logic: |

  output = ''

  if props[prop].traits['text']
    File.open("#{game.path}#{props[prop].traits['text']}", 'r') { |f|
      output << f.read
    }
  else
    output << "You look for words, but find none.\n"
  end

  output
