---
syntax:
- "drop <prop>"
- "drop <character>"

logic: |

  if character
    prop = character
  end

  output = ''

  if prop.location == 'player'
    prop.location = locations[location].name
    output << "You drop #{prop.noun}.\n"
  else
    output << (game.prop_404 prop.noun)
  end

  output
