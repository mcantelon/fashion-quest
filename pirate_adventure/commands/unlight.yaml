---
syntax:
- "unlight <prop>"

logic: |

  output = ""

  if prop.traits['lit'] == false

      output << "It's not lit.\n"

  elsif prop.traits['lit'] == true

    output << "You unlight the #{prop.name}.\n"
    prop.traits['lit'] = false

  end

  output
