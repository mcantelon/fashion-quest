---
syntax:
- "unlight <prop>"

logic: |

  output = ""

  if props[prop].traits['lit'] == false

      output << "It's not lit.\n"

  elsif props[prop].traits['lit'] == true

    output << "You unlight the #{prop}.\n"
    props[prop].traits['lit'] = false

  end

  output
