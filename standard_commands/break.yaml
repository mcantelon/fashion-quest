---
syntax:
- "hit <prop>"
- "smash <prop>"
- "break <prop>"

logic: |

  output = ''

  output = @game.event(prop, 'on_break')

  if (output == '')
    output << "Is wanton destruction really the answer?\n"
  end

  output
